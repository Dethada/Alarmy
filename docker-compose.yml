version: '2.4'

services:

  gateway:
    build: frontend/
    restart: always
    ports:
      - 8080:80
      - 8443:443

  backend:
    build: backend/
    restart: always
    depends_on:
      - postgres

  postgres:
    build: postgres/
    restart: always
    environment:
      POSTGRES_PASSWORD: verysecurepassword123
    volumes:
      - v-postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432

  agent:
    build: agent/
    privileged: true
    restart: always

volumes:
  v-postgres:
